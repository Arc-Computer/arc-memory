"""Default credentials for Arc Memory authentication.

This module contains default credentials for the Arc Memory GitHub OAuth app.
These credentials are used when no other credentials are provided.
"""

# Default GitHub OAuth credentials for the Arc organizational account
# These are embedded in the package to allow users to authenticate directly
# from the CLI without needing to provide their own OAuth credentials.
DEFAULT_GITHUB_CLIENT_ID = "YOUR_CLIENT_ID"
DEFAULT_GITHUB_CLIENT_SECRET = "YOUR_CLIENT_SECRET"
